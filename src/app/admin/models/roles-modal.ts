export interface IRoleNames{
    roleName: string
}

export interface IRoleDetails{
    roleId: number;
    roleName: string;
    description: string;
    createid: string;
    createdby: string;
    updateid: string;
    lastupdate: string
  
}